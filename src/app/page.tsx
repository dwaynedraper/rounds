// Phase 0 placeholder. Phase 1 builds the real design system (plan §9)
// before any real page gets built on top of it.
export default function Home() {
  return (
    <div className="flex flex-1 flex-col items-center justify-center gap-2 p-8 text-center">
      <h1 className="text-2xl font-semibold">Rounds</h1>
      <p className="max-w-md text-sm text-zinc-600">
        Scaffolding in progress — see <code>docs/ROUNDS-PLAN.md</code> for the
        build order.
      </p>
    </div>
  );
}
